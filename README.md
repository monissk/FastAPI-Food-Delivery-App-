# FastAPI Food Delivery App

This project is a backend system built using FastAPI.

Features:
- GET APIs
- POST with validation
- CRUD operations
- Cart and checkout workflow
- Search, sorting, pagination

Run:
uvicorn main:app --reload
